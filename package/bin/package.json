{
  "private": true,
  "dependencies": {
    "knowledgebasetech": "^0.0.2"
  },
  "scripts": {
    "dev": "cd .knowledgeBase && npm run dev",
    "build": "cd .knowledgeBase && npm run next build",
    "start": "cd .knowledgeBase && npm run next start",
    "export": "cd .knowledgeBase && npm run export",
    "init-a": "knowledgebasetech"
  }
}
